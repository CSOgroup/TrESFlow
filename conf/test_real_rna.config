/*
 * Optional external/local real-RNA validation profile.
 * This keeps the current RNA-only workflow in real mode and is intended for tiny local datasets.
 * It does not set a default samplesheet, because the real validation data is expected to live outside the repo.
 * Current boundary: through grouped FqToSAM outputs only.
 */

params {
    outdir = "${projectDir}/results/test_real_rna"
}

timeline {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/execution_timeline.html"
    overwrite = true
}

report {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/execution_report.html"
    overwrite = true
}

trace {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/execution_trace.tsv"
    overwrite = true
}

dag {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/flowchart.html"
    overwrite = true
}
