/*
 * Real-RNA validation profile for the current RNA core path.
 * RNA resources can come from the hierarchical samplesheet or CLI overrides.
 */

params {
    outdir = "${projectDir}/results/test_real_rna"
}
